

export const RepeatingText = ({content}) => {
    return (
        <p>
            {content ?? 'DYOR has never been so important: allow us to build the necessary tools to assist you.'}
        </p>
    )
}